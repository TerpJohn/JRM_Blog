@model JRM_Blog.Models.Comment

@{
    ViewBag.Title = "Details";
}
<div class="container">
<h2>Details</h2>

<div>
    <h4>Comment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Author.DisplayName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Author.DisplayName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Post.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Post.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Body)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Body)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Created)
        </dt>

        <dd>
            @Model.Created.ToString("MMM dd, yyyy HH:mm")
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Updated)
        </dt>

        <dd>
            @if (Model.Updated == null)
            {
                <p> No updates yet.</p>
            }
            else
            {
                @Model.Updated.Value.ToString("MMM dd, yyyy HH:mm")
            }
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UpdateReason)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UpdateReason)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
    @if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
    {
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) 
    }
</p>
    </div>